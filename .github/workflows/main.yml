name: C25 EMA Agent

on:
  schedule:
    - cron: "30 20 * * 1-5"   # ✅ 22:30 dansk tid (CEST)
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance pandas

      - name: Run agent
        run: python C25_LargeCap_OverEMA100.py
        env:
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
